apiVersion: v1
kind: Service
metadata:
  name: accounts-db
  namespace: bank-of-anthos
spec:
  selector:
    app: accounts-db
  ports:
  - protocol: TCP
    port: 5432
    targetPort: 5432
