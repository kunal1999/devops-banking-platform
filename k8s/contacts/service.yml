apiVersion: v1
kind: Service
metadata:
  name: contacts
  namespace: bank-of-anthos
spec:
  type: ClusterIP
  selector:
    app: contacts
  ports:
  - name: http
    protocol: TCP
    port: 8081
    targetPort: 8081
